/** Placeholder — T-009 (wave 2) owns the real simulation index (legacy redirected to
 *  /simulation/flood; wave 2 can restore that behavior). */
export default function SimulationIndexPage() {
  return (
    <div className="flex h-full flex-col items-center justify-center gap-1 p-8 text-center">
      <h1 className="text-lg font-semibold">Mô phỏng</h1>
      <p className="text-sm text-muted-foreground">Trang đang chuyển sang Vite SPA.</p>
    </div>
  );
}
