/** Placeholder — T-009 (wave 2) owns the real AQI simulation. */
export default function AQISimulationPage() {
  return (
    <div className="flex h-full flex-col items-center justify-center gap-1 p-8 text-center">
      <h1 className="text-lg font-semibold">Kịch bản Chất lượng Không khí</h1>
      <p className="text-sm text-muted-foreground">Trang đang chuyển sang Vite SPA.</p>
    </div>
  );
}
