/** Placeholder — T-009 (wave 2) owns the real flood simulation. */
export default function FloodSimulationPage() {
  return (
    <div className="flex h-full flex-col items-center justify-center gap-1 p-8 text-center">
      <h1 className="text-lg font-semibold">Kịch bản Ngập lụt</h1>
      <p className="text-sm text-muted-foreground">Trang đang chuyển sang Vite SPA.</p>
    </div>
  );
}
