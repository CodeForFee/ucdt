"""PDIM Stage-1: coefficients (constants.py) and pure response functions.

Nothing in this package performs I/O — every function takes its inputs as
arguments, so the same code scores a live snapshot, a what-if and a test fixture.
"""
