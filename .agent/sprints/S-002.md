---
id: S-002
goal: Prototype matches Figure 2 of the manuscript box for box (spec S-002-spec.md), then §4.2 + end of §3.1 + Data statement of docs/PO-UCDT draft(4).docx are written from the same spec
owner: claude-opus-5 (tech lead)
opened: 2026-09-24T0330Z
status: open
spec: .agent/sprints/S-002-spec.md
---

## Why
User 2026-09-24: "bản thảo sao thì prototype phải vậy, prototype sao thì bản thảo thế" — Figure 2
is authoritative ("phải làm theo cái hình … làm 100% matching"); model inputs may change from
the legacy (user approved). Reviewer: simulations show no recommendations; three simulators
under-described. No district names anywhere. Keyless/free data only ("không muốn mất phí").

## Rules
- Same git flow as S-001: issue → `feat/T-1NN-*` → PR into `dev` → lead review (re-run verify)
  → squash-merge only on the UNPIPED `gh pr checks --watch` exit 0. Never delete branches,
  never touch `main`.
- The spec is normative. A seam that finds the spec wrong stops and reports; the lead edits
  the spec first, then the code.
- Seam owners edit only their scope, their task file and their own log.
- Waves are gated (full suites on dev) but do not stop for the user; the lead reports one
  line per wave.

## Waves
| wave | tasks | gate |
|---|---|---|
| 1 | T-101 static layers + toponyms · T-102 ingestion (per-unit weather, AirGradient) | pytest green; derived.json committed with provenance; no district name in any unit |
| 2 | T-103 processing: per-unit PDIM, rules π(r,i), alerts per unit, counterfactual, Algorithm 1 | pytest green; worker run on compose writes snapshots per spec |
| 3 | T-104 API + contracts + gateway | pytest + bun test green; contracts regenerated |
| 4 | T-105 web | web lint/test/build; headless check of every Figure-2 box |
| 5 | T-106 manuscript (lead) | draft(4).docx: §3.1 end, §4.2 complete, Data statement — cross-checked line by line against the spec and code |

## Wave log
