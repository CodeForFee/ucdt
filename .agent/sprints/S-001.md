---
id: S-001
goal: UCDT v2 — monorepo ucdt/ (web Vite SPA + gateway Bun/Hono + climate Python FastAPI/arq + Postgres/PostGIS + Redis + Caddy) with full parity to Hackathon-BE/FE, deployable on a 2 vCPU / 3.78 GB VPS via docker compose
owner: claude-opus-5 (tech lead)
opened: 2026-09-23T1617Z
status: closed
closed: 2026-09-24T0005Z
plan: C:\Users\NEO\.claude\plans\ok-b-t-u-b-t-cryptic-tiger.md
repo: github.com/CodeForFee/ucdt (private)
---

## Rules
- A wave admits only tasks with disjoint file scopes. One task = one GitHub issue = one branch `feat/T-NNN-<slug>` = one PR **into `dev`**. `main` is the user's; agents never touch it.
- Seam owners do not edit `.agent/BOARD.md`, do not add dependencies (declared in T-001; ask the lead), do not touch files outside scope.
- Merge only on the UNPIPED exit status of `gh pr checks <n> --watch` (a pipe into tail masked a red check on #33).
- Tech lead reviews each PR (`gh pr diff`, re-runs its verify), merges with `gh pr merge --squash` (never `--delete-branch`, never delete branches — user rule 2026-09-23) only on green CI + reproduced verify. Never `--admin`.
- Each wave ends with a join gate on `dev`, then STOP for user approval.
- Legacy repos `Hackathon-BE`, `Hackathon-FE` are read-only references.

## Waves
| wave | tasks | join gate |
|---|---|---|
| 0 | T-001 | `pnpm install && uv sync`; compose postgres+redis healthy; parity.json generated; `main` pushed |
| 1 | T-002 T-003 T-004 T-005 (+ T-011 after merge) | `uv run pytest` + `bun test` + `pnpm -r build lint test` green on dev; MAP.md written |
| 2 | T-006 T-007 T-008 T-009 | all suites green; worker produces real snapshots on compose dev |
| 3 | T-010 | full stack `docker compose up`; every page works; SSE live; legacy FE works against new gateway; RAM < 1.2 GB |
| 4 | T-012 T-013 | CI green on dev; all tasks done with `## Test` |

## Wave log
<!-- one line per wave: wave N/4 · k tasks · verified: <cmd> → <result> · PRs: #a #b · next: T-… -->
- wave 0/4 · 1 task · verified: uv sync + ruff → pass; pnpm parity → 695 cases + 11 scenarios; compose → postgres+redis healthy; CI on main → 3/3 success · commit 9c96276 (direct, the only one) · branch protection unavailable on private Free repo → convention · next: T-002 T-003 T-004 T-005 (awaiting user approval)
- wave 1/4 · 4 tasks + T-011 · verified on dev 3b07ec2: `uv run pytest` → 123 passed; `bun test` → 26 pass + tsc ok; `pnpm -r lint test build` → web 19 passed, build ok; CI 3/3 · PRs: #16 #17 #18 #19 (+ #21 B-013 fix, found at the gate) · MAP.md 56 nodes · next: T-006 T-007 T-008 T-009 (awaiting user approval)
- wave 2/4 · 4 tasks · verified on dev 7a222f3 + gate fixes: `uv run pytest` → 166 passed; `bun test` → 26 pass + tsc; web lint + 34 tests + build; live chain worker → climate-api → gateway :3002 → vite: 5× /latest 200 (cache MISS→HIT), alerts, history, simulation 200 / invalid 400, SSE 5× snapshot.updated; browser: dashboard, flood, simulation render (map needs a real Mapbox token) · PRs: #23 #24 #27 #29 · found B-015 (fixed), B-014 #26, B-016 (user) · next: T-010 (awaiting user approval)
- wave 3/4 · T-010 · verified: `docker compose -f infra/compose.yml up -d --build --wait` → 7 services healthy (migrate exit 0); via Caddy http://localhost: /api/{weather,aqi,flood,heat,recommend,alerts,history} 200, SPA + deep links 200, zstd, immutable /assets; SSE through Caddy unbuffered (redis PUBLISH → browser refetch /api/alerts, no reload); legacy Hackathon-FE (NEXT_PUBLIC_API_BASE_URL=http://localhost) renders dashboard + runs POST /api/simulation/run (CORS preflight 204); map + simulation render with a real Mapbox token; docker stats total ≈ 213 MiB (< 1.2 GB) · PR: T-010 · next: T-012 T-013 (awaiting user approval)
- wave 4/4 · T-012 T-013 · verified on dev 9cd795b: `uv run pytest` → 166 passed + ruff; `bun test` → 26 pass + tsc; web lint + 37 tests + build; `bash -n` 4 scripts; `compose -f compose.yml -f compose.prod.yml config -q` ok; CI 6/6 incl. contracts drift + 3 image builds; first GHCR push ucdt-climate/ucdt-gateway :dev + :sha-6fb3532 (web skipped: no VITE_MAPBOX_TOKEN secret); backup → restore into a scratch DB → 63 units; restore under a held connection · PRs: #36 #37 (+ #33 B-016, #35 flaky test) · review caught: missing exec bits (exit 126 on Linux), dropdb vs live connections · **sprint S-001 closed**
