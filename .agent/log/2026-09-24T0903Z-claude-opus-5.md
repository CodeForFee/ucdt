# 2026-09-24T0903Z — claude-opus-5 (subagent) — T-105 web Figure-2 presentation layer (#50)

Branch `feat/T-105-web` from dev ceac7c6.

## Did
- Web types switched to `@ucdt/contracts` (tsconfig path, type-only; no package.json change).
- Flood: served decomposition rendered (city + per zone), web flood-weight copy deleted (B-014).
- Heat: sliders start at served ρ₀ / G₀; ΔG now measured against served G₀ (was G − 0.30).
- AQI: AirGradient observed stations on the map layer, the AQI simulator and the AQ page;
  simulator point markers use served `results.stations`.
- Recommendations: own tab `/recommendations` (5-tab nav, user requirement mid-task); toponym,
  rule, π, inputs, firedCount; commune never rendered.
- Alerts: header bell dropdown with unread badge, mark-read, empty state; dashboard card removed.
- Maturity card (Algorithm 1 line 8) on the dashboard; `useMaturity` + SSE invalidation on aqi.
- "Khuyến nghị cho kịch bản" counterfactual panel in all three simulators.
- Name guard test over dashboard, recommendations, bell, flood, stations, 3 simulators (vi/en).

## Verified
lint 0, tsc -b 0, vitest 86/86, build 0. Live via vite dev → http://localhost, headless Chrome
(CDP): every page renders live S-002 data with no administrative name; map tiles not checked
(no real Mapbox token).

## Left
- `infra/web.Dockerfile` must also copy `packages/contracts` (tsc -b needs it) — outside scope.
- `Alert` has no unitId/unitName; the bell parses the §F id + joins `/api/units`.
- Board: replace the 4-tab nav Decision with the 5-tab one (lead).
