# 2026-09-24T0322Z claude-opus-5 — T-102 per-unit weather + AirGradient (#47)

Branch `feat/T-102-ingest` off `origin/dev`. Scope: `services/climate/climate/ingest/**`,
`services/climate/tests/ingest/**`.

## What was done
- `weather.py`: `fetch_forecast_multi` (one Open-Meteo request for N coordinates, list/object
  handled, order kept), `unit_points`, `fetch_weather_by_unit` (dedupe 64 → 41 coordinates,
  per-location B-012 hour alignment via `transform_forecast`, malformed location → city fallback).
  `fetch_forecast` kept, now a one-coordinate call of the multi fetch.
- `airgradient.py`: `fetch_airgradient` per spec §I.3; `names.py`: the §A.1 guard.
- 24 new tests (respx).

## Verified
- ruff check + format --check clean; `uv run pytest -q` 191 passed.
- Live: 41 coordinates in one request, 0.95 s, hour 10:00 local at 03:22Z; AirGradient 1 HCMC
  station (CMT8, pm25 20.3), 11.1 s for the world payload.

## Deviations / open questions
- "Malformed" also covers a location with an empty `hourly.time` (transform_forecast would
  otherwise emit invented defaults 32 °C / 75 %).
- AirGradient name: `publicLocationName`, then `locationName`; an empty name also gets the
  generic `Trạm AirGradient <id>`.
