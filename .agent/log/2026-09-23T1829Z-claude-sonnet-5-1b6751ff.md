---
agent: claude-sonnet-5
session: 1b6751ff
started: 2026-09-23T1740Z
ended: 2026-09-23T1830Z
scope: apps/web/src/features/{dashboard,flood,air-quality,alerts}/**, apps/web/src/messages/{vi,en}.json (dashboard/cards/risk/banner/chart/airQualityPage/floodPage/alertsPage/aqi namespaces only), .agent/tasks/T-008-web-dashboard-flood-aq-alerts.md
branch: feat/T-008-web-dashboard-flood-aq-alerts
status: done
---

## Did
Ported T-008: dashboard, flood, air-quality and alerts screens from the read-only legacy
`E:/Github/Hackathon/Hackathon-FE/src/features/{dashboard,flood,air-quality,alerts}` into
`apps/web/src/features/*` (Vite + react-router + use-intl), replacing the placeholder pages
already wired into `router/routes.tsx`. `next/link`→`Link`, `next-intl`→`use-intl`, dropped
`"use client"`, added `export default` (required by `router/lazyPage.tsx`).

New files (see task file `.agent/tasks/T-008-web-dashboard-flood-aq-alerts.md` `## Execute` for
the full list): `dashboard/{pages/DashboardPage,components/*,lib/weatherOutlook(.test)}.tsx/.ts`,
`flood/{pages/FloodPage,components/RiskDecomposition,lib/decomposeFloodRisk(.test)}.tsx/.ts`,
`air-quality/pages/AirQualityPage.tsx`, `alerts/pages/AlertsPage.tsx`. Added `apps/web/.env.local`
(gitignored, `VITE_MAPBOX_TOKEN=pk.placeholder` + `VITE_API_BASE_URL=` — needed even for the
build's own zod check to not throw at runtime; note both keys must exist, an empty value alone
without the key present still fails `import.meta.env` lookup).

Extracted two pure functions out of components (required by
`react-refresh/only-export-components`, which forbids a component file from also exporting a
constant/function) and unit-tested them:
- `flood/lib/decomposeFloodRisk.ts` — R_f = w1·P̃ + w2·T̃ + w3·Ĩ − w4·D̃, all 4 terms with weight
  (B-011), drainage negative (B-010). Weights are a display-only local mirror of
  `services/climate/climate/pdim/constants.py PDIM_S1.flood`; the score itself is never
  recomputed, always read from `useFloodRisk()`.
- `dashboard/lib/weatherOutlook.ts` — B-007: real fix, not a carry-over. Calls
  `useHistory<WeatherData>("weather", 24)`; shows a genuine 24h delta only when it returns
  entries (oldest-first per `shared/hooks/useHistory.ts`), else falls back to the 6h forecast
  outlook the API already returns. Never a hardcoded number either way.

Found and fixed (same scope, same pattern as B-009, so fixed directly rather than filed as a
new open bug): `airQualityPage.source` said "OpenAQ / MONRE" in both message catalogues, but
`services/climate/climate/pdim/aqi.py::raw_from_open_meteo` sources AQI from Open-Meteo, not
OpenAQ/MONRE. Changed to "Open-Meteo · PDIM Giai đoạn 1"/"...Stage 1" to match the flood page.

DP3 enforcement while porting: dropped the legacy `getRiskLevel(score)` and ad hoc
`aqi.category === 'good'` string-matching (both absent from `apps/web/src/shared` on purpose —
see `shared/constants/riskLevels.ts`'s comment). Every `RiskBadge` level now reads a string the
API already computed (`FloodData.overallRisk`, `FloodArea.riskLevel`). AQI colour-banding uses
`shared/lib/aqi.ts` (`aqiCode`/`aqiColor`, the EPA-breakpoint source already built for this)
instead of re-declaring thresholds or trusting the backend's locale-baked `category` string
(confirmed live against the legacy server: `category` comes back as `"Trung bình"` — Vietnamese
text hardcoded server-side — so the legacy `aqi?.category === 'good'` comparison in
`OverviewCards.tsx` was already dead code against the real backend; not carried over).

Restructured `FloodPage`/`AirQualityPage` so the `<h1>` header renders unconditionally instead
of being gated behind `isLoading` (legacy returned a bare `<LoadingSkeleton/>` with no heading
at all while loading) — `router/routes.test.tsx` (out of scope, not touched) asserts each
route's heading appears, and with real network calls now firing that would have timed out.

New optional `observedAt`/`stale` fields on `/latest` payloads: read via a local
`WithFreshness<T>` intersection type in `OverviewCards.tsx`, `FloodPage.tsx`,
`AirQualityPage.tsx` rather than editing `shared/types/*` (out of scope). Renders a small amber
"stale" tag only when `stale === true`; tolerates absence (confirmed against the legacy backend,
which doesn't send it).

Renamed `AIRecommendPanel` → `RecommendPanel` (B-005 — the engine is rule-based, not AI; the
copy already said so in the message catalogue from an earlier session).

## Verified
```
$ pnpm -F web lint
> eslint .
(clean)

$ pnpm -F web test
 Test Files  6 passed (6)
      Tests  25 passed (25)

$ pnpm -F web build
> tsc -b && vite build
✓ 2837 modules transformed.
✓ built in 5.48s
```

Manual run: port 3001 was free (no other agent's backend running), started legacy
`Hackathon-BE` (`pnpm dev`) in the background — confirmed listening via `netstat`. Started
`pnpm -F web dev --port 5181 --strictPort` in the background — confirmed listening. Used the
Browser tool to load and screenshot all four routes against the real proxied API:

- `/dashboard` — all 4 overview cards (AQI 87/Trung bình, temp 24.7°C, rain 0.0mm, flood
  23/100 Thấp), weather trend charts, AQI/Heat/Flood summary cards, alerts card, and the
  rule-based recommendation panel (showing `π = 0.00 · R-NORM-00`) all rendered with live data.
  `outlook.kind` correctly resolved to `"forecast"` (network tab confirmed
  `GET /api/history/weather?hours=24 → 404` against the legacy backend, which has no history
  endpoint — B-007's fallback path, exercised for real, not just in the unit test).
- `/flood` — 18 zones by toponym, `DataSourceTag` reading "Open-Meteo · PDIM Giai đoạn 1", and
  the `RiskDecomposition` sum (`0.000 + 0.150 + 0.150 − 0.068 = 0.232`) exactly matched the
  API's `riskScore: 0.232` with no clamp note — confirms B-011 end to end against a live payload.
- `/air-quality` — gauge, pollutant bars, 23 stations each correctly banded/labelled via
  `aqiCode`/`aqiT` from the raw numeric AQI (e.g. station AQI 122/111 → "Không tốt cho nhóm nhạy
  cảm", AQI 49 → "Tốt" — correct EPA bands, independent of the backend's Vietnamese `category`
  string).
- `/alerts` — tabs, severity badge, correct icon for `type: "system"` (legacy's `TYPE_ICONS` had
  a dead `"general"` key that never matched the real `Alert["type"]` union; fixed to `"system"`).

`read_console_messages` showed only the expected `404`s (`/api/history/weather` — absent on the
legacy backend) and no uncaught exceptions after the env fix below. Stopped only the two
processes I started (`taskkill` on the vite and Hackathon-BE PIDs); never touched anything I
didn't start.

## Bugs
- Found: `apps/web/src/messages/{vi,en}.json` `airQualityPage.source` claimed "OpenAQ / MONRE";
  actual AQI data source is Open-Meteo (`services/climate/climate/pdim/aqi.py`
  `raw_from_open_meteo`). Same false-provenance pattern as B-009, in my own scope, fixed
  directly in this PR rather than filed as a separate open bug.
- Found, not fixed (outside scope, needs T-006/T-007): the legacy `Hackathon-BE`'s
  `/api/aqi` response's `category` field is a hardcoded Vietnamese label ("Trung bình") rather
  than a locale-independent code — an English build would show Vietnamese text if it ever
  trusted that field. Worked around in this PR by deriving the AQI band from the raw `aqi`
  number via `shared/lib/aqi.ts` instead of trusting `category`, so nothing in scope is affected,
  but the underlying backend field is still wrong. climate-api (T-007) should make sure its own
  `category`/equivalent field is either locale-independent or omitted in favor of a code.

## Decisions
- Extracted `decomposeFloodRisk`/`weatherOutlook` as pure, unit-tested functions in a feature
  `lib/` rather than inline in the component — required by ESLint's
  `react-refresh/only-export-components` (a component file can't also export a constant/
  function), and it's what let me test the B-010/B-011/B-007 logic without rendering.
- Kept the PDIM flood weights (w1..w4, rainRefMmH) as a small local, display-only constant in
  `flood/lib/decomposeFloodRisk.ts` rather than a shared `pdim.ts` (which Board Decision
  2026-09-23 explicitly says not to reintroduce). Justification is in the module's own doc
  comment: it only explains the already-served `riskScore`, using API-supplied `triggers`; the
  score itself is never recomputed. This mirrors what `climate/pdim/flood.py`'s own `triggers`
  comment expects the frontend to do ("All four R_f terms (B-011) must be reproducible from
  these").
- Dropped `RiskBadge` from the AQI mini-card in `OverviewCards` (legacy mapped 6 AQI categories
  onto a 4-value RiskLevel via ad hoc string comparisons against a field that, per the live
  backend, is Vietnamese text) — the `AQIGauge`'s own colour+label already conveys this
  correctly from the numeric value.

## Not done
- `shared/types/{weather,aqi,flood,heat}.ts` don't declare `observedAt`/`stale` — out of scope
  (shared/**), worked around with a local `WithFreshness<T>` intersection type per file that
  needs it. Listed in the task file's Handoff as "hoist to shared" once T-007 finalizes the
  contract.
- A tiny stale-tag helper is duplicated (2-3 lines) across `dashboard/components/OverviewCards`,
  `flood/pages/FloodPage`, `air-quality/pages/AirQualityPage` rather than shared, for the same
  reason (features can't import each other, shared is out of scope). Also in Handoff.
- Did not chase a minor responsive-layout wrap on the AQI page's "Chỉ số AQI hiện tại" card
  title at narrow (~800px) viewport widths — present in the legacy markup too (same
  `Card`/`CardHeader` classes), not a functional/DP3 issue, not touched.

## Left / next
None — task is `done`, exit condition met (`pnpm -F web lint/test/build` all green, manual run
verified against the real legacy backend). PR opened into `dev` per the git flow; tech lead
reviews and merges.
