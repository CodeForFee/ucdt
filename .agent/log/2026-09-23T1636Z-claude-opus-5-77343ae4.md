---
agent: claude-opus-5
session: 77343ae4
started: 2026-09-23T1617Z
ended: 2026-09-24T0010Z
scope: whole repo (tech lead, sprint S-001) — ucdt/**, .agent/**
branch: main (9c96276, initial scaffold), dev (56f73f3 via PR #15)
status: done
---

## Task

Người dùng: đánh giá backend có nên làm microservices không; chọn stack; "refactor lại ra 1 source hoàn toàn mới cho cả be lẫn fe" theo quy trình godkit đầy đủ; làm việc như một team trên GitHub. Chốt: monorepo `ucdt/` → `CodeForFee/ucdt` (private); FE là Vite SPA theo cấu trúc `KLTN_dev-v2/frontend`; **mỗi task một issue, một nhánh, PR vào `dev`, tech lead tự merge vào `dev`; `main` là của người dùng**; `.agent/` nằm trong repo.

## Did

- Kiến trúc v2: Caddy → gateway (Bun + Hono) → climate-api (FastAPI) + climate-worker (arq) → Postgres/PostGIS + Redis. Python sở hữu toàn bộ công thức PDIM; web bỏ `pdim.ts`; gateway không đụng DB. Plan: `C:\Users\NEO\.claude\plans\ok-b-t-u-b-t-cryptic-tiger.md`.
- Sprint S-001: 5 wave, 13 task (T-001..T-013), mỗi task có file trong `.agent/tasks/` và một GitHub issue (#2..#14).
- T-001 (wave 0), commit trực tiếp duy nhất lên `main` (`9c96276`): workspace pnpm với dependency khai sẵn, port constants PDIM + 63 đơn vị không gian, `tools/gen-parity.ts` → `parity.json` (695 pure cases + 11 kịch bản, đủ 10/10 luật và 7/7 nhánh cảnh báo), compose postgis + redis, CI khung, PR template, CODEOWNERS.
- Tạo nhánh `dev`, các label, 14 issue. PR #15 (issue #1): chuyển `.agent/` vào repo, sửa scope các task về tính từ gốc repo, CI chạy khi push lên `dev`, ghi luồng làm việc vào tài liệu → merge vào `dev`. Đóng #1 và #2.
- Dispatch wave 1 song song, mỗi agent một worktree riêng: T-002 (opus, #3), T-003 (opus, #4), T-004 (sonnet, #5), T-005 (sonnet, #6).
- Lưu quy ước làm việc với git vào memory (`git-team-workflow`).

## Verified

- `uv run ruff check . && uv run ruff format --check . && uv lock --check` → pass
- `pnpm parity` → 695 pure cases, 11 scenarios
- `docker compose -f infra/compose.yml -f infra/compose.dev.yml up -d --wait` → postgres + redis healthy, PostGIS 3.4
- CI trên `main` (9c96276): climate/gateway/web success; PR #15: 3/3 pass → squash-merged vào `dev` (56f73f3)

## Bugs

- B-012 (mở): `transformOpenMeteoResponse` của legacy khớp giờ hiện tại theo UTC với giờ địa phương của Open-Meteo → lệch 7 giờ. Fix nằm trong T-006.

## Decisions

- Luồng git: issue → `feat/T-NNN-*` → PR vào `dev` → lead review + squash → đóng issue; `main` do người dùng promote. Đã ghi lên BOARD và README.
- Branch protection không dùng được (repo private trên gói Free, HTTP 403) → quy ước do lead giữ.
- `.agent/` nằm trong repo; mỗi agent commit file task và log của mình trong chính PR của nó; lead commit board trong PR gate cuối mỗi wave.
- Các Decisions kiến trúc (Python là nguồn duy nhất của PDIM, gateway không đụng DB, gateway chạy Bun) sẽ ghi lên board ở gate W1, theo plan.

## Not done / caveats

- 3 log cũ (2026-08-19T0900Z-antigravity, 2026-09-14T0730Z/0750Z-claude-opus-5) biến mất khỏi `.agent/log/` ở gốc workspace ngay trong phiên này, trước khi chuyển thư mục. Không phải do lệnh nào ở đây xóa, và không có trong Recycle Bin. Đã ghi chú trên board.
- Chưa có `.agent/MAP.md`: để T-011 làm sau gate W1.
- Board local đang ghi T-002..T-005 = execute; thay đổi này chưa commit (sẽ vào PR gate W1).

## Left / next

Wave 1 đang chạy nền. Khi mỗi PR về: `gh pr diff`, chạy lại lệnh verify của task, `gh pr review`, rồi `gh pr merge --squash --delete-branch` vào `dev` khi CI xanh, và đóng issue. Nếu CI của T-003 thiếu service postgres thì merge T-002 trước (T-002 thêm service đó vào `climate.yml`), rồi rebase T-003. Sau cả 4 PR: gate W1 trên `dev` (`uv run pytest`, `bun test`, `pnpm -r build lint test`), T-011 map, PR gate (board + Decisions), rồi DỪNG chờ người dùng duyệt wave 2.

## Continuation 2026-09-24 (same session 77343ae4, after a session-limit stop; user: "check and continue")

(Rewritten once: the first draft of this section was lost to a `git reset --hard origin/dev` on the lead checkout before it was committed.)

### Found on resume
- The T-004/T-005 subagents had died on a session limit. A parallel session (1b6751ff, now idle) had already closed waves 1–3 (PRs #18–#31 into `dev`; B-012/B-013/B-015 fixed; T-011 map; T-010 full stack ≈ 213 MiB). My stale checkout held only changes already in `dev` and was reset to `origin/dev`.
- Rules from that session and memory: never delete branches; merge with `--squash` only.

### Did
- User decisions (2026-09-24): B-016 → drop from UI; "auto-delete head branches" → turned OFF (it had removed `dev` on the #25 promotion); Mapbox CI secret → set from infra/.env.
- The secret was NOT set: the permission gate blocked reading the token file (credential materialization). Left for the user.
- B-016: #32 → PR #33 (ComparePanel + i18n; regression test fails 3/3 on the old component). Opened B-017.
- **Process error (mine):** #33 merged while `web` was red — merge chained after `gh pr checks … | tail`, which masks the exit status. Cause was a flaky test (cold lazy route 1137 ms > findBy 1 s); identical code passed 37/37 on the next dev push. Fixed: #34 → PR #35 (4 s timeout); merges now gated on the unpiped exit status (board + sprint rule).
- Wave 4 in parallel worktrees: T-012 #36 (concurrency/caching, contracts drift check, images.yml → GHCR) merged after update-branch, 6/6 green; first push published ucdt-climate + ucdt-gateway :dev/:sha-6fb3532, web skipped (no secret). T-013 #37: round 1 changes requested (all scripts 100644 → exit 126 on Linux; restore.sh dropdb vs live connections), round 2 re-verified by me (100755; entrypoint runs on a Linux fs; stop/--force/trap restart) and merged.
- W4 gate PR: map PARTIAL refresh (82 nodes / 107 edges, 0 dangling; renderer verified to round-trip the previous MAP.md byte-for-byte first), board (T-012/T-013 done, claims cleared, 3 W4 Decisions, merge-gating rule, waiting-on-user secret), sprint S-001 wave-4 line + closed, `.claude/` in .gitignore.

### Verified
- Gate on dev 9cd795b: `uv run pytest -q` → 166 passed + ruff; `bun test` → 26 pass + tsc; `pnpm -F web lint/test/build` → clean / 37 passed / built; `bash -n` on 4 scripts; `docker compose -f infra/compose.yml -f infra/compose.prod.yml config -q` → ok
- `git archive <T-013 head> infra/backup infra/deploy.sh | docker run -i alpine …` → -rwxrwxr-x, entrypoint runs (pg_dump not found in bare alpine), no exit 126
- images run on dev push → pushing manifest ghcr.io/codeforfee/ucdt-{climate,gateway}:dev + :sha-6fb3532

### Not done / caveats
- `VITE_MAPBOX_TOKEN` secret (user) → ucdt-web not yet on GHCR, so a VPS deploy with `UCDT_TAG=main` also needs a promotion to main first.
- DEPLOY.md and deploy.sh are unproven on a real VPS (no-SSH rule).
- Stale subagent worktrees remain under `.claude/worktrees/` (branches kept per the no-delete rule).
- Open: B-014 (#26), B-017.

### Left / next
User: set the Mapbox secret, promote dev → main, deploy per docs/DEPLOY.md. Next sprint candidates: B-014 (serve decomposed R_f terms from climate), B-017 (text), prune worktrees (not branches) if the user wants.
