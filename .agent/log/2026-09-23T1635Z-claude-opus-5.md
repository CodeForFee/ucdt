---
agent: claude-opus-5
started: 2026-09-23T1635Z
ended: 2026-09-23T1650Z
scope: services/climate/{alembic.ini,climate/db/**,tests/db/**}, .agent/tasks/T-003-db-schema-repo.md
branch: feat/T-003-db-schema-repo
status: done
---

## Task
T-003 (issue #4): Postgres/PostGIS schema, Alembic migration 001 with the 63 seeded spatial units, async repository for wave-2 (T-006 worker, T-007 API), tests against `ucdt_test`.

## Did
- alembic.ini + async env.py (URL from `config.attributes["url"]` or settings).
- Migration 001: postgis extension, spatial_units (geography Point 4326, seeded from ALL_UNITS), weather_obs, aqi_obs, risk_snapshots, alerts, 4 indexes, CHECKs, FK set null.
- models.py (Core tables, tiny Geography type), session.py (cached engine/sessionmaker), repo.py (9 functions, plain dicts, never commit).
- tests/db: 9 tests incl. downgrade base -> upgrade head.

## Verified
- `uv run alembic upgrade head` on compose DB -> aqi_point 23, flood_zone 18, heat_cell 22.
- `uv run ruff check . && uv run ruff format --check . && uv run pytest tests/db -q` -> All checks passed / 15 files already formatted / 9 passed.

## Not done
- CI does not run pytest on this PR (climate.yml only runs ruff until T-002 adds postgres + pytest). DB tests are verified locally only.

## Left
- Windows dev: `localhost` in the default DATABASE_URL costs ~2 s per connection (::1 tried first; compose binds 127.0.0.1). Changing the default in `climate/config.py` to 127.0.0.1 is outside my scope — lead's call.
- Migration 001 seeds from live `ALL_UNITS`; freeze if units.py changes.

## next
T-006 / T-007 build on `climate/db/repo.py` — contract in the task file Handoff.
